{
  "version": 2,
  "builds": [
    {
      "src": "src/http/server.ts",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/src/http/server.ts"
    }
  ]
}